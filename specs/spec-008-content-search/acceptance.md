# SPEC-008 验收记录

- Spec：`specs/spec-008-content-search/spec.md`
- 当前 Spec 状态：Accepted
- 验收状态：PASS, User Confirmed
- 最新验收轮次：第二轮返工后独立复验 Round 4

本文件只记录实施后的独立验收结论和证据，不修改 `spec.md` 中的需求与验收标准。

## 验收门禁

- [x] 用户已明确确认完整 `spec.md`
- [x] Spec 已经过 `Confirmed` 门禁
- [x] 实施由新的单一实施 Agent `spec008_implementation_round1` 完成
- [x] 实施 Agent 未创建子 Agent
- [x] 实施 Agent 已返回报告并永久退役
- [x] 已创建身份独立的验收 Agent `spec008_acceptance_round1`
- [x] 验收 Agent 未修改业务代码
- [x] 初次独立验收实际运行一次完整 Browser E2E
- [x] 用户已确认最终验收完成

## 验收标准记录

| 编号 | 结果 | 证据与说明 |
| --- | --- | --- |
| AC-001 | PASS | `pg_trgm`、4 个 GIN 索引、空库/历史库迁移和代表性 `EXPLAIN` 通过 |
| AC-002 | PASS | Portal 渲染到 `document.body`；四入口八组几何中心偏差均为 `0px`，交互与焦点无回归 |
| AC-003 | PASS | 多字段 AND、中文子串、英文大小写、模式字符与匿名读取通过 |
| AC-004 | PASS | 四级排序、稳定游标和非法作用域游标拒绝通过 |
| AC-005 | PASS | URL、Tab、刷新返回、无频道栏和笔记卡片通过 |
| AC-006 | PASS | 视频分类可恢复且只返回正式空状态 |
| AC-007 | PASS | 用户昵称/小蓝书号、公开字段、统计和自身导航通过 |
| AC-008 | PASS | 匿名公开主页、公开字段、公开笔记、404 和自身跳转通过 |
| AC-009 | PASS | 关注、取消关注、登录恢复一次和自关注拒绝通过 |
| AC-010 | PASS | 视频 Tab 在 `299ms` 仍为骨架，`300ms` 后显示正式空状态且无伪视频请求 |
| AC-011 | PASS | `960×600` 顶部为 `48px`，无裁切、根横向溢出、假滚动或双重滚动 |
| AC-012 | PASS | 请求竞争、真实多页游标及 SPEC-007/008 同环境隔离组合回归均通过 |

## 验收过程记录

- 2026-07-28：用户确认全部关键搜索决策和视频空分类边界；
- 2026-07-28：SPEC-008 进入 `In Review`，等待用户审查完整规格；
- 2026-07-28：用户明确回复“开始实施”，SPEC-008 通过 `Confirmed` 门禁；
- 2026-07-28：创建新的单一实施 Agent `spec008_implementation_round1`，开始初次实施；
- 2026-07-28：实施 Agent 返回 `COMPLETED`，随后被永久退役；
- 实施 Agent 报告格式、Lint、类型、数据库校验、前后端 `142` 项测试和构建通过；
- 实施 Agent 报告历史库及空库迁移、`pg_trgm` 索引查询计划和 SPEC-008 Chromium E2E 通过；
- 完整 Browser E2E 按分层策略留给初次独立验收。
- 2026-07-28：创建新的独立验收 Agent `spec008_acceptance_round1`，开始逐项验收 AC-001 至 AC-012，并要求运行一次完整 Browser E2E。
- 2026-07-28：第一轮独立验收返回 `FAIL`，验收 Agent 随后被永久退役。
- 2026-07-28：创建新的单一返工 Agent `spec008_rework_round1`，只处理第一轮历史失败与直接受影响回归。
- 2026-07-29：返工 Agent 返回 `COMPLETED`，随后被永久退役。
- 2026-07-29：创建新的独立复验 Agent `spec008_reacceptance_round2`，定向复验全部历史失败与受影响回归。
- 2026-07-29：用户在复验期间发现搜索模态框相对右侧内容区居中，并明确要求改为相对完整页面在顶部水平居中；该要求已写入 Spec `1.1` 并交由当前复验 Agent 独立检查。
- 2026-07-29 15:42：`spec008_reacceptance_round2` 自创建后持续停留在 `pending_init`，超过 1 小时且没有消息、工具调用或业务文件变化；经只读检查确认后中断并永久退役，未产生验收结论。
- 2026-07-29：创建新身份复验 Agent `spec008_reacceptance_round3`，继续历史失败复验并加入模态框顶部全页面居中的强制检查。
- 2026-07-29：`spec008_reacceptance_round3` 返回 `FAIL`，随后被永久退役；历史失败已闭环，但模态框位置与最低视口裁切未通过。
- 2026-07-29：创建新的单一返工 Agent `spec008_rework_round2`，仅修复模态层 Portal、顶部全页面居中和正式几何回归。
- 2026-07-29：第二轮返工 Agent 返回 `PARTIAL` 并永久退役；代码与几何验证完成，现有 SPEC-008 Chromium 业务回归因 Docker Desktop 未运行而留给独立复验。
- 2026-07-29：创建新的独立复验 Agent `spec008_reacceptance_round4`，复验 Portal、八组正式几何场景及 SPEC-008 Chromium 业务回归。
- 2026-07-29：`spec008_reacceptance_round4` 返回 `PASS` 并永久退役；所有历史失败和用户新增的位置要求均已闭环，等待用户最终确认。

## 第一轮实施摘要

- 实施 Agent：`spec008_implementation_round1`
- 状态：`COMPLETED`
- 完成范围：数据库搜索迁移、后端搜索与公开用户 API、共享搜索模态框、搜索结果页、公开用户主页、OpenAPI、自动化测试和相关 E2E；
- 环境：Node.js `v24.9.0`，未调用 NVM，未安装其他 Node.js；
- 委派：未创建子 Agent；
- Git：未暂存、未提交、未推送；
- 临时文件：未创建项目副本；正式 E2E runner 使用的仓库内临时运行目录已清理，开发 PostgreSQL、Redis 和既有 `node_modules` 保留；
- 注意：以上是实施自测证据，不代替独立验收。

## 第一轮独立验收

- 验收 Agent：`spec008_acceptance_round1`
- 结论：`FAIL`
- 验证层级：初次独立验收；实际运行一次完整 Browser E2E；
- 静态与代码检查：
  - `git diff --check`：PASS；
  - `pnpm format:check`：PASS；
  - `pnpm lint`：PASS；
  - `pnpm typecheck`：PASS；
  - `pnpm db:validate`：PASS；
  - `pnpm test`：PASS，前端 `63` 项、后端 `79` 项；
  - `pnpm build`：PASS；
  - PostgreSQL 扩展、索引、迁移和三条代表性 `EXPLAIN`：PASS；
- 完整 Browser E2E：`134 passed / 324 skipped / 1 failed`；
- 失败一：视频 Tab 切换时直接显示“暂无视频内容”，未先保持约 `300ms` 稳定骨架；
- 失败二：搜索专属自动化没有覆盖快速切换关键词、快速切换 Tab 和旧响应晚到；
- 失败三：完整 E2E 中 SPEC-007 匿名点赞登录恢复场景一次未在 `10s` 内出现“取消点赞，当前 1”；该文件在新隔离环境定向复跑 `3/3` 通过，但必须调查并稳定时序，不能以重跑覆盖真实失败记录；
- 验收 Agent 未修改业务代码、未调用 NVM、未安装 Node.js、未创建子 Agent；
- 正式 E2E 隔离容器、网络、卷和端口已清理；用户原有开发服务、根 `node_modules` 保留。

## 第一轮返工边界

- 为视频 Tab 增加共用的约 `300ms` 分类切换骨架及定向自动化；
- 增加搜索关键词/Tab 快速切换和旧响应晚到的搜索专属自动化；
- 补强真实多页稳定游标测试；
- 调查 SPEC-007 匿名点赞登录恢复的间歇性时序并稳定实现或正式 E2E；
- 不扩大视频系统，不重写已经验收通过的搜索核心；
- 返工实施与复验按 `.ai/rules/testing.md` 运行历史失败项和受影响回归；只有触发升级条件时才再次运行完整 Browser E2E。

## 第一轮返工实施摘要

- 返工 Agent：`spec008_rework_round1`
- 状态：`COMPLETED`
- 视频骨架：新增 `300ms` 稳定切换阶段，并以定时测试验证 `299ms` 仍显示骨架、`300ms` 后显示正式空状态；
- 请求竞争：笔记首次加载接入 `AbortController`，增加快速切换关键词、快速切换 Tab 和旧响应晚到测试；
- 游标证据：正式搜索 E2E 使用真实 PostgreSQL 数据分三页验证稳定顺序、无重复遗漏及非法/跨作用域游标 `400`；
- 间歇性失败根因：SPEC-008 与 SPEC-007 复用邮箱、会话和作者，导致验证码 `60s` 冷却、验证码消耗及社交状态污染；
- 间歇性失败修复：SPEC-008 使用独立作者、访客、邮箱和会话；登录 E2E 增加验证码发送及弹框关闭的准确断言；
- 最终定向证据：
  - 前端视频/竞争相关测试 `9/9` 通过；
  - 修改范围 Lint、类型、格式与 `git diff --check` 通过；
  - 搜索与社交互动 Chromium 组合回归 `5/5` 通过；
- 完整 Browser E2E 未重复运行，依据是初次独立验收已运行完整矩阵，本轮改动限于搜索加载时序和正式测试数据隔离；
- 未调用 NVM、未安装 Node.js、未创建子 Agent、未执行 Git 写操作；
- 未创建项目副本；E2E 一次性目录与产物已清理，既有各级 `node_modules` 保留。

## 第一轮返工后独立复验

- 复验 Agent：`spec008_reacceptance_round3`
- 结论：`FAIL`
- 已通过历史失败：
  - 视频 Tab `300ms` 稳定骨架；
  - 快速关键词、快速 Tab 与旧响应晚到竞争处理；
  - 真实 PostgreSQL 多页稳定游标及非法/跨作用域游标拒绝；
  - SPEC-008 与 SPEC-007 独立测试身份，同一隔离环境组合回归 `5/5` 通过；
- 新增位置要求实际几何结果：
  - `1440×900`：首页、个人页、搜索页、公开主页的模态框中心均相对完整视口向右偏移 `116px`；
  - `960×600`：四个入口同样向右偏移 `116px`，顶部为 `-3.39px`，发生裁切；
- 根因证据：
  - `SearchDialog` 渲染在顶部搜索组件内部；
  - `.topbar` 的 `backdrop-filter` 形成固定定位的局部包含块；
  - `.search-modal-layer` 因而相对右侧 `.topbar` 定位；
  - 模态层仍使用垂直居中，而不是顶部对齐；
- 复验运行前端定向测试 `13/13`、相关 Lint/类型/格式检查和 `git diff --check`，均通过；
- 未重复完整三浏览器矩阵，依据是初次验收已完成一次完整矩阵，本轮 Chromium 已稳定证明强制位置失败；
- 复验 Agent 未修改业务代码或正式测试，未调用 NVM、未安装 Node.js、未创建子 Agent；
- 临时几何目录、E2E 产物、容器、网络、数据卷和隔离端口均已清理，既有 `node_modules` 保留。

## 第二轮返工边界

- 将共享搜索模态层通过 Portal 或等价方式渲染到 `document.body`，脱离 `.topbar` 的局部包含块；
- 模态框相对完整视口水平居中，并在顶部区域保留自适应呼吸空间；
- `960×600` 及以上不得发生顶部裁切、横向偏移、假滚动或双重滚动；
- 为首页、个人页、搜索页和公开主页补充正式几何回归，覆盖常用桌面视口和 `960×600`；
- 不修改搜索后端、视频、游标、社交互动或其他已通过范围。

## 第二轮返工实施摘要

- 返工 Agent：`spec008_rework_round2`
- 状态：`PARTIAL`，实现已完成但一个相关业务回归因本地 Docker Desktop 未运行而未执行；
- 实现：
  - 使用 React Portal 将搜索模态层渲染到 `document.body`；
  - 相对完整视口水平居中；
  - 顶部间距使用 `clamp(24px, 8dvh, 72px)`；
  - 保留遮罩、Esc、Enter、焦点、滚动锁、预填与校验行为；
  - 正式 E2E 加入四个入口、`1440×900` 与 `960×600` 的几何断言；
- 实施几何结果：
  - 八组场景中心偏差均为 `0px`；
  - `1440×900` 顶部为 `72px`；
  - `960×600` 顶部为 `48px`；
  - 无顶部/底部裁切、内部垂直溢出、根横向溢出或背景滚动失效；
- 定向证据：四页面组件测试 `35` 项、正式 Chromium 几何 E2E、修改范围格式/Lint/类型和 `git diff --check` 均通过；
- 未运行：现有 SPEC-008 Chromium 搜索业务回归，原因是正式隔离 runner 连接 Docker API 失败；没有将未运行报告为通过；
- 完整三浏览器矩阵按小范围返工策略未重复运行；
- 未调用 NVM、未安装依赖、未创建子 Agent、未执行 Git 写操作；
- 隔离前端端口、Playwright 产物和临时 Next.js 缓存已清理，既有 `node_modules` 保留。

## 第二轮返工后独立复验

- 复验 Agent：`spec008_reacceptance_round4`
- 结论：`PASS`
- Portal 与定位：
  - `SearchDialog` 独立确认使用 `createPortal(..., document.body)`；
  - 模态层使用完整视口固定定位、顶部对齐和 `clamp(24px, 8dvh, 72px)`；
  - 未发现 SSR 或 hydration 错误；
- 八组独立几何结果：
  - 首页、个人页、搜索页、公开主页在 `1440×900` 的中心偏差均为 `0px`，顶部 `72px`；
  - 四个入口在 `960×600` 的中心偏差均为 `0px`，顶部 `48px`；
  - 对话框上下完整可见，模态层覆盖完整视口，无内部溢出、根横向溢出、假滚动或双重滚动；
  - 正式 E2E 中心误差容差为 `≤2px`，可以稳定发现历史 `116px` 偏移；
- 交互与业务回归：
  - Enter、遮罩、Esc、预填、校验、焦点进入和恢复通过；
  - 四组前端定向测试 `35/35` 通过；
  - SPEC-008 Chromium 正式回归 `3/3` 通过，覆盖几何、搜索契约与浏览器业务闭环；
  - 搜索字段、排序、游标、三 Tab、用户搜索、公开主页、登录恢复和关注无回归；
- 静态检查：修改范围 Lint、前端/E2E 类型、格式和 `git diff --check` 通过；
- 分层依据：未重复完整三浏览器矩阵；初次独立验收已完成一次完整矩阵，历史第一轮失败已由 Round 3 独立关闭，本轮只改 Portal、样式和正式几何 E2E；
- 环境：Node.js `v24.9.0`，未调用 NVM、未安装 Node.js、未创建子 Agent；
- Docker：原始为关闭；复验启动既有 Docker Desktop 运行隔离 E2E，随后删除测试容器、网络、卷和产物并恢复为关闭；用户原有容器和 PostgreSQL 未受影响；
- Git：未暂存、未提交、未推送。

## 用户确认前局部 UI 修复

- 日期：`2026-07-29`；
- 用户发现登录状态下从“我”返回首页时，首页异步恢复会话前会短暂显示“登录”按钮；
- 用户明确授权协调开发 Agent 直接修复，本次不创建实施 Agent 或验收 Agent；
- 根因：首页重新挂载时用户状态初始为空，异步会话请求完成前被错误渲染为未登录入口；
- 修复：首页会话恢复增加 `250ms` 中性占位宽限期；会话在宽限期内完成时直接显示“我”，请求持续较久时仍会在宽限期后开放登录入口，避免慢网络下无法登录；
- 新增回归测试验证已登录会话恢复期间不闪现登录按钮；
- 定向验证：首页组件测试 `19/19`、修改范围 Lint、前端类型和格式检查均通过；
- 未运行完整 Browser E2E，依据是本次仅涉及首页单一会话入口的局部显示时序，定向组件回归已覆盖修改和慢会话边界。
- 2026-07-29：用户确认最终结果无问题，SPEC-008 状态更新为 `Accepted`。
