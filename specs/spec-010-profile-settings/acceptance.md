# SPEC-010 验收记录

- Spec：`specs/spec-010-profile-settings/spec.md`
- 当前 Spec 状态：Accepted
- 验收状态：Accepted
- 最新验收轮次：第五轮返工后独立复验

本文件只记录实施后的独立验收结论和证据，不修改 `spec.md` 中的需求与验收标准。

## 验收门禁

- [x] 用户已明确确认完整 `spec.md`
- [x] Spec 已经过 `Confirmed` 门禁
- [x] 实施由新的单一实施 Agent完成
- [x] 实施 Agent 未创建子 Agent
- [x] 实施 Agent 已返回报告并永久退役
- [x] 已创建身份独立的验收 Agent
- [x] 验收 Agent 未修改业务代码
- [x] 初次独立验收实际运行一次完整 Browser E2E
- [x] 独立验收结论为 `PASS`
- [x] 用户已确认最终验收完成

## 验收标准记录

| 编号 | 结果 | 证据与说明 |
| --- | --- | --- |
| AC-001 | PASS | 全新库部署 9 个迁移成功；历史库升级及旧用户、笔记、互动保留通过 |
| AC-002 | PASS | 私有接口认证和用户边界正确；公开响应未泄露邮箱、生日和对象 Key |
| AC-003 | PASS | 昵称、性别和简介校验及整体失败路径通过 |
| AC-004 | PASS | 纯日期存储、服务端年龄计算、生日当天和闰日边界通过 |
| AC-005 | PASS | 年龄默认不公开；无生日和清空生日规则正确；公开接口只返回派生年龄 |
| AC-006 | PASS | 真实格式、5 MiB、像素、动画和最低尺寸限制通过 |
| AC-007 | PASS | EXIF Orientation、裁剪坐标、512×512 WebP、元数据移除和透明背景通过 |
| AC-008 | PASS | 状态机、引用保护和真实并发探针证明正式头像及其他引用文件不会被清理 |
| AC-009 | PASS | 预留、租约、重试、双 Worker、失败补偿和持久可追踪清理通过 |
| AC-010 | PASS | 两个独立上下文、409、重载再保存和冲突头像补偿通过 |
| AC-011 | PASS | 快速成功、快速失败和持续请求的骨架行为通过；保存成功返回个人主页的定向单测和 Chromium E2E 通过 |
| AC-012 | PASS | 取消、内部导航、刷新/离开保护及焦点恢复通过 |
| AC-013 | PASS | 当前/公开主页、历史内容、评论、通知和搜索使用最新头像与昵称 |
| AC-014 | PASS | 字段错误关联、跨浏览器键盘裁剪、960×600和连续尺寸变化通过 |
| AC-015 | PASS | 字段错误运行时、OpenAPI、前端消费、安全遍历预算和全局回归均通过 |

## 验收过程记录

- 2026-07-29：用户确认个人资料设置的全部关键产品和技术边界；
- 2026-07-29：SPEC-010 完整初稿进入 `In Review`；
- 2026-07-29：用户明确同意完整 Spec，状态更新为 `Confirmed`；
- 2026-07-29：第一轮单一实施 Agent 完成实现并永久退役；
- 2026-07-29：新的独立验收 Agent 完成初次验收，结论为 `FAIL`；
- 2026-07-29：新的单一返工 Agent 完成第一轮返工并永久退役，等待新的独立验收 Agent 复验；
- 2026-07-29：新的独立验收 Agent 完成第一轮返工复验，结论仍为 `FAIL`，并已永久退役；
- 2026-07-29：新的单一返工 Agent 完成第二轮返工并永久退役，等待新的独立验收 Agent 复验；
- 2026-07-29：新的独立验收 Agent 完成第二轮返工复验，AC-008、AC-009 通过，AC-015 仍失败，并已永久退役；
- 2026-07-29：新的单一返工 Agent 完成第三轮返工并永久退役，等待新的独立验收 Agent 复验；
- 2026-08-01：第三轮首个复验实例因平台额度错误未产生验收结论；恢复后创建新的独立验收 Agent；
- 2026-08-01：新的独立验收 Agent 完成第三轮返工复验，AC-015 仍失败，并已永久退役；
- 2026-08-01：新的单一返工 Agent 完成第四轮返工并永久退役，等待新的独立验收 Agent 复验；
- 2026-08-01：新的独立验收 Agent 完成第四轮返工复验，AC-015 仍失败，并已永久退役；
- 2026-08-01：新的单一返工 Agent 完成第五轮返工并永久退役，等待新的独立验收 Agent 复验；
- 2026-08-01：新的独立验收 Agent 完成第五轮返工复验，AC-001～AC-015 全部通过，并已永久退役；
- 2026-08-01：用户追加保存后返回个人主页和自定义出生日期选择器优化，并明确授权协调 Agent 直接修复、不另建子 Agent；定向单测、Lint、类型检查、前端构建和 Chromium E2E 通过；
- 2026-08-01：用户明确确认最终验收通过，SPEC-010 标记为 `Accepted`。
- CI 不作为 Spec 验收项。

### 第一轮独立验收

- 结论：`FAIL`；
- Node.js：`v24.9.0`，未调用 NVM；
- `pnpm db:validate`：通过；
- `pnpm format:check`：通过；
- `pnpm lint`：通过；
- `pnpm typecheck`：通过；
- `pnpm test`：失败，前端 `76/76` 通过，后端 `100/101` 通过，Swagger 文档生成失败；
- `pnpm build`：通过；
- `pnpm test:e2e`：失败，完整三浏览器矩阵 `142` 通过、`370` 按矩阵跳过、`1` 失败；
- Chromium SPEC-010 完整资料闭环通过；
- Firefox、WebKit SPEC-010 关键差异场景通过，但正式覆盖仍缺少完整键盘裁剪和连续尺寸变化；
- E2E 唯一直接失败为新增“编辑资料”菜单后，旧个人页退出登录焦点回归用例未更新；
- `git diff --check`：通过，仅有现有换行提示；
- 独立 PostgreSQL、Redis、历史迁移库、媒体目录、容器、网络、Volume 和测试产物均已清理；
- 验收 Agent 未创建子 Agent、未修改业务代码、未执行 Git 写操作，并已永久退役。

### 第一轮返工要求

1. 修复 Swagger DTO 循环依赖，并让搜索、评论、公开用户等 OpenAPI 头像联合类型、年龄和简介与真实响应一致；
2. 更新个人页菜单焦点回归测试，验证“编辑资料”为首项时仍可继续操作退出登录；
3. 设计并测试即使“新头像删除失败 + 清理记录写入失败”也不会失去可重试定位信息的持久补偿；
4. 在服务端裁剪前规范化 EXIF Orientation，并增加旋转 JPEG 坐标测试；
5. 快速加载失败也必须保持约 `300ms` 骨架；
6. 补齐生日、简介、性别和公开年龄字段错误的渲染及 `aria-describedby` 关联；
7. 补齐两个浏览器上下文的版本冲突、UI 冲突恢复、带头像冲突、Chromium 媒体故障、Firefox/WebKit 键盘裁剪和连续尺寸变化 E2E；
8. 返工后由新的独立验收 Agent按历史失败项和受影响回归重新验收。

### 第一轮返工复验

- 结论：`FAIL`；
- 已确认第一轮验收中的 Swagger 循环、菜单焦点回归、EXIF 裁剪、300ms 骨架、字段错误语义和正式 E2E 覆盖均已修复；
- 后端全量 `18` 套件、`103` 项通过，相关前端 `19` 项通过；
- Prisma、格式、Lint、类型、构建和定向三浏览器 E2E 均通过；
- 定向 E2E 为 `30` 通过、`27` 按风险矩阵跳过；
- 新发现清理记录立即到期的并发竞态：清理任务可在资料事务提交前取得 Key，并在事务正式引用后删除该头像；
- 新发现 PATCH 资料接口真实校验错误为 `VALIDATION_ERROR`，但 OpenAPI 400 契约没有声明；
- 未重复完整九项目 Browser E2E，依据为初验已完成完整矩阵，本轮按返工失败项和受影响回归复验；
- E2E 专属容器、网络、Volume 和临时目录已清理；保留可复用依赖与 `frontend/.next-e2e`；
- 复验 Agent 未创建子 Agent、未修改业务代码、未执行 Git 写操作，并已永久退役。

### 第二轮返工要求

1. 为清理意图增加“预留/可清理”状态、租约或等价并发安全机制，预留状态不得被普通重试任务取得；
2. 清理任务删除对象前必须在同一安全边界内重新确认对象没有被任何用户资料正式引用；
3. 增加“清理已尝试取得 Key、保存事务随后正式引用”的确定性并发测试，并验证正式引用文件不会被删除；
4. 保留第一轮已修复的双重故障可追踪能力，防止修复竞态时重新引入静默孤立文件；
5. 将 `VALIDATION_ERROR` 及真实字段详情纳入 PATCH 400 DTO 和 Swagger 响应，并用实际 Nest 应用、文档和非法 multipart 请求验证契约一致；
6. 第二轮返工后由新的独立验收 Agent复验 AC-008、AC-009、AC-015 及直接相关回归。

### 第二轮返工复验

- 结论：`FAIL`；
- 空库部署全部 `9` 个迁移、SPEC-009 历史库增量升级和旧数据保留通过；
- 真实 PostgreSQL 与文件系统并发探针确认 `RESERVED → READY → CLEANING` 状态机、租约、双 Worker、引用保护和失败补偿正确，AC-008、AC-009 更新为 `PASS`；
- 资料、头像、媒体、Swagger 定向测试 `38/38` 通过，后端全量 `109/109` 通过；
- Prisma、格式、Lint、类型和后端构建通过；
- `VALIDATION_ERROR`、`MULTIPART_INVALID` 代码与 Swagger 已一致且无敏感信息泄露；
- 但全局字段校验响应仍没有安全字段详情，OpenAPI 也把 `details` 保持为无结构可选值，不满足字段级错误契约；
- 第二轮未改前端或 E2E，沿用第一轮三浏览器复验证据，未重复 Browser E2E；
- 专属数据库、媒体目录和探针均已清理；复验 Agent 未创建子 Agent、未修改业务代码或执行 Git 写操作，并已永久退役。

### 第三轮返工要求

1. 全局 ValidationPipe 从 `ValidationError[]` 递归提取稳定字段路径，只返回字段名/路径，不返回被拒绝的值、约束原文或内部结构；
2. `VALIDATION_ERROR` 响应必须包含结构化 `details.fields`，对非法 `gender` 至少返回 `["gender"]`；
3. Profile PATCH 400 OpenAPI DTO 将该字段详情结构化声明为真实必需字段，并保持 `MULTIPART_INVALID` 等非字段错误的契约合理；
4. 使用实际编译后的 Nest 应用、docs-json 和非法 multipart 请求同时断言响应、DTO、文档一致及无敏感信息；
5. 运行全局校验直接受影响的后端全量回归，防止认证、发布和互动等现有错误响应发生非预期破坏；
6. 第三轮返工后由新的独立验收 Agent复验 AC-015。

### 第三轮返工复验

- 结论：`FAIL`；
- 真实编译 Nest 应用、Supertest 和 docs-json 证明 `VALIDATION_ERROR`、`MULTIPART_INVALID` 及四分支 OpenAPI 契约已经一致；
- 前端同时安全消费 `details.fields` 和 `details.field`，白名单字段映射、未知字段降级、错误语义和输入保留通过；
- 后端全量 `110/110`、资料页/个人页/首页回归 `39/39`、Prisma、格式、Lint、类型及前后端构建通过；
- 正常字段路径提取的单字段、多字段、嵌套对象、数组索引、去重、排序和不安全路径过滤通过；
- 独立异常探针发现 `children=[null]` 会抛出 `TypeError`，自循环 `children` 会抛出栈溢出 `RangeError`；
- 未重复 Browser E2E，依据为本轮只修改字段错误映射，组件、真实 API 和既有三浏览器证据仍具代表性；
- 未创建临时副本或脚本；复验 Agent 未创建子 Agent、未修改业务代码或执行 Git 写操作，并已永久退役。

### 第四轮返工要求

1. 字段路径遍历在读取节点前执行运行时对象检查，忽略 `null`、标量、函数和其他非法节点；
2. 使用对象身份访问集合阻止自循环和共享循环结构重复递归；
3. 增加合理且明确的最大深度和节点数量上限，超限时安全停止，不抛异常、不泄露内部数据；
4. 保持正常字段路径、去重、排序和不安全路径段过滤行为不变；
5. 增加异常 `children`、自循环、互相循环、超深和超宽结构的正式单元测试，并运行全局校验直接回归；
6. 第四轮返工后由新的独立验收 Agent复验 AC-015。

### 第四轮返工复验

- 结论：`FAIL`；
- 非法节点、异常 getter、自循环、互循环、共享循环、合法深度和正常节点预算均已安全处理；
- 真实 Nest 响应、Swagger oneOf、前端字段错误消费、后端全量 `116/116` 和静态检查通过；
- 独立代理数组探针发现索引读取抛错时不会累计候选预算，长度 `1032` 会执行全部读取并保留先前结果，未在超过 `1024` 时整体 fail-closed；
- 独立路径探针发现 `__proto__`、`prototype`、`constructor` 等保留段没有被拒绝；
- 未重复 Browser E2E，依据为本轮仅涉及后端字段路径提取安全性；
- 临时探针已删除；复验 Agent 未创建子 Agent、未修改业务代码或执行 Git 写操作，并已永久退役。

### 第五轮返工要求

1. 将每个声明的 children 数组槽位计入访问预算，必须在读取索引 getter 前扣减预算；异常读取也不能绕过；
2. 安全读取并校验 children 长度；非有限、负数、非整数或超过剩余预算时立即整体 fail-closed，不遍历巨大长度；
3. 超过深度、对象节点或数组槽位任一预算时统一返回空字段集，不保留此前部分字段；
4. 明确拒绝 `__proto__`、`prototype`、`constructor` 等原型链保留路径段，并保持合法标识符、数字索引、去重、排序规则；
5. 正式测试覆盖巨大/异常 length、所有索引 getter 抛错、预算边界、保留段位于根/嵌套/数组之后，以及既有循环和正常路径回归；
6. 第五轮返工后由新的独立验收 Agent复验 AC-015。

### 第五轮返工复验

- 结论：`PASS`；
- AC-001～AC-015 全部通过；
- 字段路径提取定向测试 `11/11`、真实 Nest/Swagger 定向测试 `1/1`、后端全量 `120/120` 通过；
- 独立代理探针确认超预算数组不读取索引且整体 fail-closed，精确预算内每个索引最多读取一次，累计数组预算不可绕过；
- 异常 length、深度/节点边界、null、循环、共享节点和嵌套数组均安全终止；
- `__proto__`、`prototype`、`constructor` 及大小写变体在根、嵌套和数组路径后全部拒绝；
- 合法字段、数字索引、去重、排序、真实 `details.fields` 和 Swagger oneOf保持正确；
- Prisma、格式、全仓 Lint、全仓类型、后端构建和差异检查通过；
- 未重复 Browser E2E，依据为第五轮只修改后端字段路径安全逻辑，初次完整矩阵及后续跨浏览器证据仍具代表性；
- 未创建临时文件；复验 Agent 未创建子 Agent、未修改业务代码或执行 Git 写操作，并已永久退役；
- 当前等待用户明确确认最终验收，确认后将 Spec 更新为 `Accepted` 并提供中文 Conventional Commits 提交信息。
