# SPEC-016 验收记录

- Spec：`specs/spec-016-following-feed-and-social-graph/spec.md`
- 当前 Spec 状态：Accepted
- 验收状态：PASS / Accepted
- 最新验收轮次：第三轮独立复验通过

本文件只记录实施后的独立验收结论和证据，不修改 `spec.md` 的需求与验收标准。

## 验收门禁

- [x] 用户已明确确认完整 `spec.md`
- [x] Spec 已进入 `Accepted`
- [x] 实施由一个新的单一实施 Agent 完成
- [x] 实施 Agent 未创建子 Agent
- [x] 实施 Agent 已返回报告并永久退役
- [x] 已创建身份独立的验收 Agent
- [x] 验收 Agent 未修改业务代码或测试
- [x] 初次独立验收已运行一次完整 Browser E2E
- [x] AC-001 至 AC-014 全部通过
- [x] 用户已确认最终验收完成

## 验收标准记录

| 编号 | 状态 | 验收重点 |
| --- | --- | --- |
| AC-001 | PASS | 首页【关注】入口、URL 与互斥状态已通过单元及 Browser 验证 |
| AC-002 | PASS | 未登录门禁、零私人请求和登录后原 URL 加载已通过 |
| AC-003 | PASS | 图文/视频混排、历史内容、稳定排序与签名游标已通过 |
| AC-004 | PASS | 精确空状态文案与两类空状态已独立复验通过 |
| AC-005 | PASS | 当前用户关注/粉丝模态框的分页、滚动、焦点与错误状态已通过 |
| AC-006 | PASS | 匿名及登录用户公开关系列表与公开字段边界已通过 |
| AC-007 | PASS | 关注、回关、已关注、互关与自身状态已通过 |
| AC-008 | PASS | 幂等关注/取消、确认、失败保留与权威响应已通过 |
| AC-009 | PASS | 第三方拉黑下权威计数与列表口径已独立复验通过 |
| AC-010 | PASS | 关注通知、取消不通知和互关私信资格联动已通过 |
| AC-011 | PASS | 拉黑、封禁、年龄和治理过滤及服务端门禁已通过 |
| AC-012 | PASS | SPEC-015→016 永久历史迁移探针已独立复验通过 |
| AC-013 | PASS | 960×600、自适应、内部滚动和三浏览器焦点场景已通过 |
| AC-014 | PASS | 完整矩阵唯一失败的旧搜索定位器已修复并独立定向复验通过 |

## 独立验收要求

- 验收 Agent 必须独立阅读完整 Spec、计划、决策和当前差异；
- 不得以实施报告替代独立验证；
- 不得修改业务代码、测试、Spec 或验收标准；
- 不得创建子 Agent 或执行 Git 写操作；
- 必须验证真实 PostgreSQL 查询、游标绑定、过滤和必要迁移；
- 必须验证匿名、登录、退出和切换账号的数据隔离；
- 必须验证拉黑删除关系与封禁保留关系的不同恢复语义；
- 初次独立验收只运行一次完整 Browser E2E；
- 临时副本只能位于仓库根 `test/` 的任务专属目录，结束后按项目规则清理；
- 未运行项必须说明依据，不得记为通过。

## 验收轮次

### 第一轮独立验收

- 状态：FAIL
- 实施 Agent：`spec016_implementation_round1`（已返回完整报告并永久退役）
- 验收 Agent：`spec016_acceptance_round1`（已永久退役；整理正式报告时连接中断）
- 验证层级：初次独立验收；完整 Browser E2E 只正式运行一次，没有重跑。
- 独立定向结果：后端 4 套 41/41、前端 2 套 28/28 通过。
- 失败 1（AC-004）：`frontend/app/page.tsx` 的 `NO_FOLLOWS` 文案为“还没有关注任何用户”，而 Spec 要求“关注感兴趣的用户，发现他们的最新内容”。
- 失败 2（AC-009）：关系写成功后的 `followingCount` / `followerCount` 只过滤目标账号状态与年龄，未合并当前查看者的双方拉黑集合，可能与同一查看者可访问的关系列表数量不一致。
- 失败 3（AC-012）：`e2e/run-e2e.mjs` 的历史迁移探针只应用到 SPEC-015 迁移，未应用并断言 `20260813000100_add_follower_pagination_index`，无法永久证明 SPEC-015 历史库无损升级到 SPEC-016。
- 失败 4（AC-014）：完整 Browser E2E runner 已正式启动并执行 finally 清理，但后台结果流未返回可恢复的 Playwright 汇总或退出码；`e2e/test-results` 为空，因此不得记为通过，也没有在相同工作区状态重跑。
- 后续边界：创建新的单一返工实施 Agent，只修复上述失败并运行定向测试/迁移探针；返工阶段不运行完整 Browser E2E。返工后由新的独立验收 Agent复验全部失败项和受影响回归；由于最终工作区会发生相关变化且可信完整结果缺失，复验阶段允许且只允许正式运行一次完整 Browser E2E。

### 第一轮返工实施

- 状态：COMPLETED
- 返工 Agent：`spec016_rework_round1`（已返回完整报告并永久退役）
- AC-004：精确空状态文案与永久前端断言已修正，前端定向测试 22/22 通过。
- AC-009：权威计数已合并查看者和列表所有者的第三方拉黑集合，后端定向测试 10/10 通过。
- AC-012：永久 runner 已实际应用 SPEC-016 迁移并验证索引、约束、关注、通知、会话和私信历史无损；独立迁移探针模式通过。
- 返工验证：三项目 typecheck、受影响 lint/format、`git diff --check` 通过；按门禁未运行完整 Browser E2E。

### 第二轮独立复验

- 状态：FAIL
- 验证范围：AC-004、AC-009、AC-012 历史失败项及受影响回归；因最终工作区已变化且上一轮完整结果不可恢复，本轮正式且只运行一次完整 Browser E2E。
- 历史失败复验：AC-004、AC-009、AC-012 均通过；前端 28/28、后端 42/42、迁移探针 exit 0。
- 完整 Browser E2E：九项目矩阵正式运行一次，181 passed、556 skipped、1 failed，exit 1；SPEC-016 专属 Chromium 流程通过。
- 唯一失败：`e2e/tests/content-search.spec.ts` 使用 `getByRole('button', { name: '关注' })`，同时匹配公开主页主关注按钮和新增关注统计按钮，Playwright strict mode 在业务请求前终止。
- 返工边界：新的单一返工 Agent 只将旧 E2E 定位器限定到主关注按钮；不得修改业务代码，不得运行完整 Browser E2E。新的独立复验 Agent只运行该历史失败场景与直接相关公开主页关注回归，不再运行完整 E2E，并复用本轮 181/556/1 的完整结果作为全套证据。

### 第二轮测试返工

- 状态：COMPLETED
- 返工 Agent：`spec016_rework_round2`（已返回完整报告并永久退役）
- 修改范围：仅 `e2e/tests/content-search.spec.ts`，把公开主页主关注按钮定位器改为精确可访问名称匹配；未修改业务代码。
- 定向结果：`chromium-1440` 历史失败场景 1/1 通过；E2E 文件级 lint、typecheck、Prettier 与差异检查通过。
- 验证边界：未运行完整 Browser E2E；临时测试目录与产物已清理。

### 第三轮独立复验

- 状态：PASS
- 验证范围：只独立复验 AC-014 唯一历史失败场景和定位器边界；复用第二轮完整矩阵 181 passed / 556 skipped / 1 failed 的全套结果，不再次运行完整 Browser E2E。
- 静态结论：`e2e/tests/content-search.spec.ts` 只为公开主页主关注按钮定位器增加 `exact: true`，登录、验证码、关注成功、历史返回和公开主页跳转断言均未弱化。
- 定向结果：`chromium-1440` 历史失败场景通过；没有运行完整 Browser E2E、整个测试文件或额外 Firefox/WebKit。
- 复用证据：第二轮完整矩阵的 181 个通过项与 SPEC-016 专属流程结果继续有效；唯一失败已在最终代码状态独立复验通过，因此 AC-001 至 AC-014 全部 PASS。
- 执行偏差：验收 Agent 首次定向运行遗漏任务目录环境变量，随后在未发生代码变化时为满足临时目录要求重复运行同一场景；两次均通过。没有重复完整 E2E，但违反“相同状态不重复定向验证”的效率门禁，已如实记录。
- 清理：`test/spec016-acceptance-round3`、误用默认目录 `test/browser-e2e-local`、本轮容器/网络/卷、`e2e/test-results/.last-run.json` 均已清理；未触碰用户其他测试目录。

## 用户最终确认

- 2026-08-14：用户明确确认“没有问题了”，同意 SPEC-016 验收完成并标记为 `Accepted`。
