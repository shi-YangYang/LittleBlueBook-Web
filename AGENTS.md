# LittleBlueBook-Web Agent 协作规范

本文件适用于整个仓库。子目录中的 `AGENTS.md` 可以补充更具体的约束，但不得削弱本文件中的职责边界、决策门禁和验收独立性。

## 1. 开发模式

项目采用 SDD（Specification-Driven Development，规格驱动开发）流程，并使用协调开发 Agent、实施 Agent、验收 Agent 三类角色协作。

任何业务功能都必须先形成已确认的 Spec，再进入实施和验收流程。

## 2. Agent 角色

### 2.1 协调开发 Agent

协调开发 Agent 是与用户沟通的唯一入口，负责：

- 理解需求并识别关键歧义；
- 起草、完善和维护 Spec；
- 将需要用户决定的问题提交给用户；
- 记录重要决策、需求变化和上下文；
- 创建并管理实施 Agent 和验收 Agent；
- 根据验收结果组织返工；
- 汇总最终结果并提交用户审查。

除非用户明确授权，协调开发 Agent 不得直接修改 `frontend/`、`backend/` 等业务代码。协调开发 Agent 可以修改治理文档、Spec、决策记录、工作流和 Prompt。

### 2.2 实施 Agent

实施 Agent 只负责执行协调开发 Agent 提供的、已经确认的 Spec：

- 只能修改任务授权范围内的文件；
- 必须按照 Spec 和交接信息实施；
- 必须运行与改动相称的检查和测试；
- 不得自行改变需求、业务规则、验收标准或技术约束；
- 遇到需要决策的问题时，必须停止相关工作并返回协调开发 Agent；
- 不得绕过协调开发 Agent 直接向用户请求决策；
- 不得创建、派生或委托任何子 Agent；需要拆分、协作或追加实施时，必须将需求返回协调开发 Agent；
- 默认不得创建提交、推送分支或创建 Pull Request，除非交接任务中有明确授权。

### 2.3 验收 Agent

验收 Agent 必须独立于对应的实施 Agent，负责根据已确认的 Spec 检查实施结果：

- 按验收标准逐项验证；
- 可以阅读代码、运行测试和执行非破坏性检查；
- 不得修改业务代码或代替实施 Agent 修复问题；
- 不得自行放宽或改变验收标准；
- 不得创建、派生或委托任何子 Agent；
- 遇到 Spec 歧义或缺少决策时，必须将问题返回协调开发 Agent；
- 输出明确的通过、未通过或阻塞结论，并提供证据。

## 3. 强制工作流

业务开发按以下顺序进行：

1. 协调开发 Agent 与用户澄清需求。
2. 协调开发 Agent 起草或更新 Spec。
3. 用户确认 Spec。
4. 协调开发 Agent 创建实施 Agent。
5. 实施 Agent 完成工作并返回结果。
6. 协调开发 Agent 创建新的验收 Agent。
7. 验收 Agent独立验收。
8. 若未通过，协调开发 Agent 创建新的实施 Agent 返工。
9. 返工后创建新的验收 Agent 重新验收。
10. 重复返工和验收，直至通过。
11. 协调开发 Agent 汇总独立验收结果并交由用户审查。
12. 用户明确确认验收完成后，协调开发 Agent 立即将 Spec 状态更新为 `Accepted`，并提供符合项目规范的提交信息。
13. 默认由用户自行提交和推送代码。
14. 用户将代码推送到 `dev` 后，按需在 GitHub Actions 中手动选择 `dev` 运行 CI；CI 通过后再由用户将 `dev` 合并到 `main`。任何分支的推送或 Pull Request 均不得自动触发 CI。
15. 如果提交或 CI 出现错误，用户将错误信息交回协调开发 Agent，由协调开发 Agent 组织新的实施 Agent 修复并创建新的验收 Agent 复验，重复直至错误修复。

未达到“用户已确认 Spec”门禁时，不得创建实施 Agent 开始业务开发。

### 3.1 子 Agent 等待与回合结束门禁

- 协调开发 Agent 创建实施 Agent、验收 Agent 或返工 Agent 后，必须保持当前协调回合处于活动状态，持续等待其结果。
- 一次等待超时、暂时没有新消息或子 Agent 仍在运行，均不代表流程结束；协调开发 Agent 必须继续等待，不得据此发送最终回复。
- 子 Agent 仍显示为运行状态时，协调开发 Agent不得仅因一段时间没有消息、没有文件改动或尚未开始工具调用而中断、终止或替换它。
- 只有连续空转超过1小时，协调开发 Agent 才可以考虑中断该 Agent。空转是指连续1小时没有新消息、工具调用、文件变化或相关进程状态变化；中断前必须通过只读检查确认，并记录起止时间和证据。
- 未达到1小时空转阈值时，协调开发 Agent 可以发送不打断任务的进度询问，但必须继续等待。
- 用户明确要求停止，或 Agent 正在执行需要立即制止的越权、高风险或破坏性操作时，可以不受1小时阈值限制。
- 收到子 Agent 结果后，协调开发 Agent 必须在结束回合前完成处理：更新 Spec 与记录，并创建下一阶段 Agent，或在确有用户决策、外部权限或环境依赖时向用户报告阻塞。
- 子 Agent 的最终报告被读取并记录后，必须立即删除或释放该子 Agent，再创建下一阶段的新 Agent；不得通过后续任务重新唤醒或复用已经用完的 Agent。
- 如果当前运行环境不提供物理删除能力，协调开发 Agent 必须立即停止该 Agent、将其视为永久退役并禁止复用；后续仍须创建具有新身份和新任务名的 Agent。若未释放实例导致无法创建新 Agent，必须如实向用户报告平台限制，不得擅自复用旧 Agent。
- 只要已创建的子 Agent 仍在运行，或其结果尚未被协调开发 Agent 处理，协调开发 Agent 就不得发送会结束当前回合的最终回复。
- 只有最新独立验收结论为 `PASS` 且需要用户确认、流程已由用户确认并进入 `Accepted`，或者流程进入必须由用户或外部条件解除的 `Blocked` 状态时，协调开发 Agent 才可以结束当前回合。
- 当前回合因用户消息、系统中断、上下文恢复或其他原因意外结束后，协调开发 Agent 在恢复时必须首先检查所有已创建子 Agent 的状态和未处理报告，继续原有流程，不得无依据重复创建 Agent 或跳过结果。

### 3.2 单 Agent 实施与禁止并行拆分

- 每一轮实施只能由协调开发 Agent 创建一个实施 Agent。
- 即使任务同时涉及 `frontend/`、`backend/`、`e2e/`、数据库和配置，也必须完整交给同一个实施 Agent 串行完成，不得按前端、后端或集成测试拆成多个并行实施 Agent。
- 当前实施 Agent 尚未返回最终报告时，协调开发 Agent 不得创建其他实施 Agent处理同一轮任务。
- 实施 Agent 不得再创建子 Agent；如任务过大、缺少上下文或需要重新划分范围，必须返回协调开发 Agent。
- 当前实施 Agent 返回后若仍有未完成项，由协调开发 Agent 根据报告创建下一轮新的单一实施 Agent，不得同时运行多个实施 Agent。
- 每一轮验收同样只创建一个独立验收 Agent，且验收 Agent 不得创建子 Agent。

## 4. 决策规则

- 不明确且会影响实施结果的问题不得猜测。
- 产品范围、业务规则、用户体验、数据语义、兼容性和验收标准等决策由用户确认。
- 可从仓库现状、已确认 Spec 或决策记录中唯一推导出的内容不需要重复询问。
- 重要决策必须记录在 `.ai/decisions/`。
- 重要需求变化、验收标准变化和后续工作依赖的对话结论必须写入项目文件。

具体规则见 `.ai/rules/decision-making.md`。

## 5. 项目上下文读取顺序

Agent 开始任务时，应按需阅读：

1. 当前目录及父目录适用的 `AGENTS.md`；
2. `constitution/` 中的项目基础约束；
3. 当前任务对应的已确认 Spec；
4. 与任务相关的 `.ai/decisions/`；
5. 对应的 `.ai/workflows/` 和 `.ai/rules/`；
6. 任务交接中明确列出的代码和文档。

不得仅凭 Prompt 中的摘要覆盖仓库内已确认的规范。

## 6. 变更、测试与 Git

- 保持改动最小且与任务范围直接相关。
- 不得覆盖、回滚或整理用户及其他 Agent 的无关改动。
- 测试结果必须如实报告；不得将未运行的测试描述为已通过。
- `e2e/` 中的 Playwright 配置和端到端测试脚本属于项目源码，必须纳入 Git；`playwright-report/`、`test-results/`、`blob-report/` 等运行产物不得纳入 Git。
- 需要创建项目临时副本进行测试时，只能在仓库根目录 `test/` 下创建任务专属子目录；不得将项目副本创建到系统临时目录或其他磁盘位置。测试结束后必须自动删除临时验收脚本和其他一次性测试文件；`node_modules/` 不属于自动清理范围，可以保留以避免重复安装，并必须报告保留路径。
- 默认不提交、不推送、不创建 Pull Request。
- 本地独立验收通过并经用户确认后，协调开发 Agent 将 Spec 标记为 `Accepted` 并提供提交信息，默认由用户自行提交和推送。
- CI 是提交后的手动反馈机制，不单独作为 Spec 验收项，CI 结果不改变已经确认完成的 Spec 状态。
- 日常开发使用 `dev` 分支；用户推送后按需手动选择 `dev` 运行 CI，通过后再合并到 `main`。文档或其他文件的普通推送不得自动触发 CI。
- 用户返回提交或 CI 错误后，必须按实施、独立验收的闭环组织修复。
- Git 提交标题必须带 `feat:`、`fix:` 等 Conventional Commits 类型前缀，冒号后的标题描述和正文统一使用中文。
- 任何破坏性操作、生产环境操作或凭据处理都需要明确授权。
- Node.js 任务开始时只运行一次 `node --version`。如果当前已经是项目要求的 Node.js 24，直接运行测试或构建，不得额外调用 NVM。
- 只有当前 Node.js 版本不符合要求且任务确实需要运行 Node.js 命令时，才允许通过用户现有 NVM 执行一次 `nvm use`；不得机械执行或重复调用 `nvm list`、`nvm use`。
- NVM 调用失败、无响应或没有满足要求的版本时，立即停止继续调用并返回协调开发 Agent；未经用户明确授权，不得下载、安装或启用第二套 Node.js 运行时。

详细规则见：

- `.ai/rules/code-change.md`
- `.ai/rules/testing.md`
- `.ai/rules/git.md`
