# Used only by server-side frontend code. Do not prefix secrets with NEXT_PUBLIC_.
BACKEND_URL=http://127.0.0.1:3001

# Public REST base URL used by browser requests. This value must not contain secrets.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3001/api/v1
