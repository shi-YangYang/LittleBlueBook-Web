# 子 Agent 任务交接模板

## 身份

- Agent 角色：实施 / 验收
- 任务名称：
- 任务轮次：
- 上一轮 Agent：如适用

## 权威依据

- Spec 路径：`specs/spec-NNN-short-name/`
- Spec 状态与确认依据：
- Constitution：
- 相关决策：
- 适用工作流：
- 适用规则：

## 任务目标

用可验证的语言描述本轮必须完成的结果。

## 范围

### 范围内

-

### 范围外

-

## 验收标准

逐项引用 Spec 中的验收标准：

1.

## 验证层级

- 当前阶段：实施过程 / 初次实施交付 / 初次独立验收 / 返工实施 / 返工复验
- 必跑定向测试：
- 必跑相关回归：
- 明确不运行的检查及依据：
- 是否运行完整 Browser E2E：是 / 否
- 完整 Browser E2E 的运行或不运行依据：
- 浏览器范围：Chromium / Chromium + Firefox + WebKit 关键场景 / 其他已确认范围
- 可复用的既有验证结果：
- 验证升级条件：

除非上述验证升级条件实际发生，子 Agent 不得自行把定向任务升级为全仓测试、完整构建或完整 Browser E2E。

## 仓库上下文

- 相关目录和文件：
- 当前工作区状态：
- 已知已有改动：
- 可复用命令或工具：
- 可复用的健康 Docker 服务、已安装依赖、浏览器或本地进程：
- 临时测试副本：不需要 / `test/<任务专属子目录>/`
- 临时副本自动清理方式：删除临时脚本和一次性文件；保留 `node_modules/` 并报告路径

## 权限

- 允许读取：
- 允许修改：
- 允许运行：
- Git 权限：
- 外部系统权限：
- 明确禁止：
  - 创建、派生或委托子 Agent；
  - 未经用户授权安装另一套 Node.js；先检查一次 `node --version`，仅在版本不符时通过现有 NVM 尝试一次切换，不得重复调用 NVM。

## 必须返回

- 结论或完成状态；
- 验收标准逐项结果；
- 改动文件或检查范围；
- 已运行命令及结果；
- 未运行检查及原因；
- 本轮验证层级及是否运行完整 Browser E2E 的依据；
- 是否避免了重复运行同一工作区状态下已经通过的检查；
- 风险、限制和阻塞；
- 临时测试副本的路径、用途、一次性文件清理结果及保留的 `node_modules/` 路径；
- 需要协调开发 Agent 处理的决策。
